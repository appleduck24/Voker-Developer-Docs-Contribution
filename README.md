# Voker-Developer-Docs-Contribution
A Python hotel booking agent built with Voker's SDK. Uses two tool calls for availability checks and bookings, Smart Skills to clarify room type before booking, and runs through a correction scenario where a guest fixes a wrong room type. Voker automatically detects the correction event and logs the resolution with no custom logging required.
